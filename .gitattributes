# Normalize to LF regardless of OS — keeps scripts runnable and avoids diff noise.
* text=auto eol=lf
*.sh text eol=lf

# Lockfiles are generated; mark them for cleaner GitHub reviews.
mise.lock linguist-generated=true
pnpm-lock.yaml linguist-generated=true
